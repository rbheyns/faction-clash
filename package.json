{
  "name": "faction-clash",
  "private": true,
  "version": "1.0.0",
  "description": "Faction Clash — browser card game (single-file index.html) plus regression tests.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
